import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      50: "#e6f7ff",
      100: "#b3e0ff",
      500: "#0099ff",
      900: "#005c99",
    },
  },
});

export default theme;